require'nvim-tree'.setup {
  git = {
    ignore = false,
  },
  update_focused_file = {
    enable = true,
  },
  renderer = {
    indent_markers = {
      enable = true,
    },
    add_trailing = true
  },
}
